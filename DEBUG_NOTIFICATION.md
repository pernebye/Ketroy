# 🔍 Диагностика проблемы с уведомлениями

## Симптомы
- Пользователь нажимает "Нет" на модалке подтверждения
- Уведомление приходит в любом случае

## Что проверить

### 1. DevTools Console (F12 → Console tab)
При активации новости должны видеть логи:

**Если выбран "Да":**
```
📢 Отправка уведомления с фильтрами: {
  newsId: 123,
  newsName: "Название новости",
  filters: { ... }
}
🔇 Уведомление НЕ отправляется - пользователь выбрал "Нет"
```

**Если выбран "Нет":**
```
🔇 Уведомление НЕ отправляется - пользователь выбрал "Нет"
```

### 2. DevTools Network (F12 → Network tab)
Посмотри параметры запроса PATCH к `/admin/news/{id}`:

**Правильно (Да):**
```json
{
  "is_active": true,
  "send_notification": true,
  "target_cities": ["Almaty", "Astana"],
  ...
}
```

**Правильно (Нет):**
```json
{
  "is_active": true
}
```
(без `send_notification`)

## Вероятные причины

### 🔴 На фронте
- [ ] Модалка закрывается без вызова `onReject` (нужно проверить `onDialogClose`)
- [ ] `sendNotification` всегда `true` (но кода это видно не должно быть)

### 🔴 На бэкенде (БОЛЕЕ ВЕРОЯТНО)
- [ ] Бэкенд игнорирует флаг `send_notification` и отправляет уведомление всегда
- [ ] Бэкенд проверяет `send_notification` неправильно (например, проверяет что это не `false` вместо проверки что это `true`)
- [ ] Уведомление отправляется в другом месте (например, в другом эндпоинте или хуке)

## Решение

### На фронте (если проблема там)
Модалка уже `persistent`, поэтому её нельзя закрыть кликом вне.

### На бэкенде (если проблема там)
В методе `quickUpdate` для новостей добавить проверку:

```php
if ($request->has('send_notification') && $request->send_notification) {
    // Отправить уведомление
    sendNewsNotification($news, $request->all());
} else {
    // НЕ отправлять уведомление
}
```

**Важно:** использовать `$request->has('send_notification')` и `$request->send_notification === true`

## Проверка от пользователя

1. Открыть DevTools (F12)
2. Перейти на вкладку Console
3. Активировать новость
4. Выбрать "Нет"
5. Посмотреть логи в Console
6. Зайти на вкладку Network
7. Посмотреть параметры последнего PATCH запроса
8. Скинуть скриншот параметров

Это поможет определить, где именно проблема.


